// Vitest setup file. Intentionally minimal for now: jsdom is already wired by
// vite.config.js, and component-level Testing Library helpers will be imported
// per-test (once @testing-library/dom is added as a dep) rather than globally.
